diff --git a/apps/flutter/lib/main.dart b/apps/flutter/lib/main.dart
--- a/apps/flutter/lib/main.dart
+++ b/apps/flutter/lib/main.dart
@@ -10,6 +10,7 @@ import 'package:shared_preferences/shared_preferences.dart';
 import 'package:url_launcher/url_launcher.dart';
 
 import 'drive_controller.dart';
+import 'file_preview.dart';
 import 'diagnostics.dart';
 import 'google_auth.dart';
 import 'models.dart';
@@ -595,7 +596,7 @@ class _Sidebar extends StatelessWidget {
                         ),
                         Text(
-                          'Version 21.1 Test',
+                          'Version 21.2 Test',
                           style: TextStyle(
                             color: Color(0xff9db5d1),
                             fontSize: 12,
@@ -626,6 +627,11 @@ class _Sidebar extends StatelessWidget {
               const SizedBox(height: 28),
               _DriveTile(
+                leading: const NativeFileIcon(
+                  fileName: 'All Drives',
+                  isFolder: true,
+                  size: 24,
+                ),
                 title: 'All Drives',
                 subtitle: 'Unified view',
                 quota: controller.totalStorageLimit == null
@@ -649,6 +655,11 @@ class _Sidebar extends StatelessWidget {
                 child: ListView(
                   children: controller.accounts
                       .map((account) => _DriveTile(
+                            leading: NativeFileIcon(
+                              fileName: account.name,
+                              isFolder: true,
+                              size: 24,
+                            ),
                             title: account.name,
                             subtitle: account.email,
                             subtitleColor:
@@ -765,6 +776,7 @@ class _DriveTile extends StatelessWidget {
     required this.subtitle,
     required this.selected,
     required this.onTap,
+    this.leading,
     this.subtitleColor,
     this.quota,
     this.onDisconnect,
@@ -773,6 +785,7 @@ class _DriveTile extends StatelessWidget {
   final String subtitle;
   final bool selected;
   final VoidCallback onTap;
+  final Widget? leading;
   final Color? subtitleColor;
   final String? quota;
   final VoidCallback? onDisconnect;
@@ -782,6 +795,7 @@ class _DriveTile extends StatelessWidget {
         selected: selected,
         selectedTileColor: const Color(0xff203a57),
         shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(12)),
+        leading: leading,
         title: Text(title,
             overflow: TextOverflow.ellipsis,
             style: const TextStyle(
@@ -1514,9 +1528,20 @@ class _FileListState extends State<_FileList> {
       clipBehavior: Clip.antiAlias,
       child: LayoutBuilder(
         builder: (context, constraints) {
-          final tableWidth = requiredWidth > constraints.maxWidth
+          final previewItem = controller.selectedItems.length == 1
+              ? controller.selectedItems.single
+              : null;
+          final showPreview = previewItem != null &&
+              !previewItem.isFolder &&
+              constraints.maxWidth >= 900;
+          final listWidth = showPreview
+              ? constraints.maxWidth - 340
+              : constraints.maxWidth;
+          final tableWidth = requiredWidth > listWidth
               ? requiredWidth
-              : constraints.maxWidth;
-          return Scrollbar(
+              : listWidth;
+          final table = Scrollbar(
             controller: _horizontalScroll,
             thumbVisibility: true,
             notificationPredicate: (notification) => notification.depth == 1,
@@ -1611,7 +1636,8 @@ class _FileListState extends State<_FileList> {
                           message: '${item.location} / ${item.name}',
                           waitDuration: const Duration(milliseconds: 350),
                           child: InkWell(
-                            onTap: () => _openItem(context, item),
+                            onTap: () => controller.selectOnly(item),
+                            onDoubleTap: () => _openItem(context, item),
                             child: Padding(
                               padding: const EdgeInsets.symmetric(vertical: 6),
                               child: Column(
@@ -1766,11 +1792,28 @@ class _FileListState extends State<_FileList> {
                     ],
                   ),
-                  onTap: () => controller.toggle(
-                    item,
-                    !controller.selectedKeys.contains(controller.keyOf(item)),
-                  ),
-                  onLongPress: () => _openItem(context, item),
+                  onTap: () => controller.selectOnly(item),
                 ),
                 const Divider(height: 1),
               ],
@@ -1780,6 +1823,19 @@ class _FileListState extends State<_FileList> {
               ),
             ),
           );
+          if (!showPreview) return table;
+          return Row(
+            children: [
+              Expanded(child: table),
+              const VerticalDivider(width: 1),
+              SizedBox(
+                width: 339,
+                height: constraints.maxHeight,
+                child: FilePreview(
+                  controller: controller,
+                  item: previewItem,
+                  onOpen: () => _openItem(context, previewItem),
+                ),
+              ),
+            ],
+          );
         },
       ),
     );
diff --git a/apps/flutter/lib/models.dart b/apps/flutter/lib/models.dart
--- a/apps/flutter/lib/models.dart
+++ b/apps/flutter/lib/models.dart
@@ -57,6 +57,7 @@ class DriveItem {
     this.size,
     this.modifiedTime,
     this.webViewLink,
+    this.thumbnailLink,
     this.parents = const [],
     this.canDownload = true,
     this.ownedByMe = false,
@@ -70,6 +71,7 @@ class DriveItem {
   final int? size;
   final DateTime? modifiedTime;
   final String? webViewLink;
+  final String? thumbnailLink;
   final List<String> parents;
   final bool canDownload;
   final bool ownedByMe;
@@ -86,6 +88,7 @@ class DriveItem {
         size: size,
         modifiedTime: modifiedTime,
         webViewLink: webViewLink,
+        thumbnailLink: thumbnailLink,
         parents: parents,
         canDownload: canDownload,
         ownedByMe: ownedByMe,
@@ -108,6 +111,7 @@ class DriveItem {
         size: int.tryParse('${json['size'] ?? ''}'),
         modifiedTime: DateTime.tryParse('${json['modifiedTime'] ?? ''}'),
         webViewLink: json['webViewLink'] as String?,
+        thumbnailLink: json['thumbnailLink'] as String?,
         parents: (json['parents'] as List?)?.cast<String>() ?? const [],
         canDownload:
             (json['capabilities'] as Map?)?['canDownload'] as bool? ?? true,
diff --git a/apps/flutter/lib/google_drive_api.dart b/apps/flutter/lib/google_drive_api.dart
--- a/apps/flutter/lib/google_drive_api.dart
+++ b/apps/flutter/lib/google_drive_api.dart
@@ -65,7 +65,7 @@ class GoogleDriveApi {
         'pageSize': '1000',
         'orderBy': 'folder,name_natural',
         'fields':
-            'nextPageToken,files(id,name,mimeType,size,modifiedTime,webViewLink,parents,ownedByMe,capabilities(canDownload))',
+            'nextPageToken,files(id,name,mimeType,size,modifiedTime,webViewLink,thumbnailLink,parents,ownedByMe,capabilities(canDownload))',
         if (pageToken != null) 'pageToken': pageToken,
       };
@@ -91,7 +91,7 @@ class GoogleDriveApi {
         'spaces': 'drive',
         'pageSize': '1000',
         'fields':
-            'nextPageToken,files(id,name,mimeType,size,modifiedTime,webViewLink,parents,ownedByMe,capabilities(canDownload))',
+            'nextPageToken,files(id,name,mimeType,size,modifiedTime,webViewLink,thumbnailLink,parents,ownedByMe,capabilities(canDownload))',
         if (pageToken != null) 'pageToken': pageToken,
       };
diff --git a/apps/flutter/lib/drive_controller.dart b/apps/flutter/lib/drive_controller.dart
--- a/apps/flutter/lib/drive_controller.dart
+++ b/apps/flutter/lib/drive_controller.dart
@@ -601,6 +601,10 @@ class DriveController extends ChangeNotifier {
     }
   }
 
+  Future<Uint8List> previewBytes(DriveItem item) =>
+      api.downloadBytes(accountById(item.accountId)!, item);
+
   Future<void> paste() => _guard(() async {
         final clip = clipboard;
         final destination = selectedAccount;
diff --git a/apps/flutter/pubspec.yaml b/apps/flutter/pubspec.yaml
--- a/apps/flutter/pubspec.yaml
+++ b/apps/flutter/pubspec.yaml
@@ -1,7 +1,7 @@
 name: farooqdrive
 description: FarooqDrive multi-account Google Drive file manager.
 publish_to: none
-version: 21.1.0+22
+version: 21.2.0+23
 
 environment:
   sdk: ">=3.5.0 <4.0.0"
@@ -33,7 +33,7 @@ msix_config:
   identity_name: MohammadFarooq.FarooqDrive
   publisher: CN=88632D82-E648-416A-A69B-DD8EADB81121
-  msix_version: 21.1.0.0
+  msix_version: 21.2.0.0
   logo_path: assets/FarooqDrive.png
   architecture: x64
   capabilities: internetClient
