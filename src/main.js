const{app,BrowserWindow,ipcMain,dialog,shell,safeStorage}=require('electron');const{DatabaseSync}=require('node:sqlite');const http=require('http'),https=require('https'),fs=require('fs'),path=require('path'),crypto=require('crypto');let db,win;
const SCOPE='openid email profile https://www.googleapis.com/auth/drive';
const enc=s=>s?safeStorage.encryptString(String(s)).toString('base64'):null,dec=s=>s?safeStorage.decryptString(Buffer.from(s,'base64')):'';
function init(){db=new DatabaseSync(path.join(app.getPath('userData'),'farooqdrive.sqlite'));db.exec(`PRAGMA foreign_keys=ON;CREATE TABLE IF NOT EXISTS settings(k TEXT PRIMARY KEY,v TEXT);CREATE TABLE IF NOT EXISTS accounts(id INTEGER PRIMARY KEY,email TEXT UNIQUE,name TEXT,access TEXT NOT NULL,refresh TEXT,expires INTEGER,root TEXT,quota INTEGER,used INTEGER DEFAULT 0);CREATE TABLE IF NOT EXISTS folders(id INTEGER PRIMARY KEY,name TEXT NOT NULL,parent INTEGER);CREATE TABLE IF NOT EXISTS files(id INTEGER PRIMARY KEY,account INTEGER NOT NULL,driveid TEXT NOT NULL,name TEXT NOT NULL,mime TEXT,size INTEGER DEFAULT 0,folder INTEGER,parent TEXT,modified TEXT,view TEXT,UNIQUE(account,driveid),FOREIGN KEY(account) REFERENCES accounts(id) ON DELETE CASCADE);`)}
function secret(k){let r=db.prepare('SELECT v FROM settings WHERE k=?').get(k);return r?dec(r.v):''}function setSecret(k,v){db.prepare('INSERT INTO settings(k,v) VALUES(?,?) ON CONFLICT(k) DO UPDATE SET v=excluded.v').run(k,enc(v))}function cfg(){return{clientId:secret('clientId'),clientSecret:secret('clientSecret')}}
function rawAccounts(){return db.prepare('SELECT * FROM accounts ORDER BY id').all()}function pubAccounts(){return rawAccounts().map(a=>({id:Number(a.id),email:a.email,name:a.name,quota:a.quota===null?null:Number(a.quota),used:Number(a.used||0)}))}function state(){let c=cfg();return{oauthConfigured:!!(c.clientId&&c.clientSecret),accounts:pubAccounts(),folders:db.prepare('SELECT id,name,parent FROM folders ORDER BY name').all().map(x=>({id:Number(x.id),name:x.name,parent:x.parent===null?null:Number(x.parent)})),files:db.prepare('SELECT f.*,a.email FROM files f JOIN accounts a ON a.id=f.account ORDER BY f.name').all().map(x=>({id:Number(x.id),account:Number(x.account),driveid:x.driveid,name:x.name,mime:x.mime,size:Number(x.size||0),folder:x.folder===null?null:Number(x.folder),modified:x.modified||'',view:x.view||'',email:x.email}))}}
async function jf(url,opt={}){let r=await fetch(url,opt),t=await r.text(),b;try{b=t?JSON.parse(t):null}catch{b=t}if(!r.ok)throw Error(`Google API ${r.status}: ${b?.error?.message||b?.error_description||t||r.statusText}`);return b}
function account(id){let a=db.prepare('SELECT * FROM accounts WHERE id=?').get(id);if(!a)throw Error('Drive account not found.');return a}async function token(id){let a=account(id);if(a.expires>Date.now()+60000)return dec(a.access);let c=cfg(),refresh=dec(a.refresh);if(!refresh)throw Error(`Reconnect ${a.email}; refresh token is missing.`);let b=new URLSearchParams({client_id:c.clientId,client_secret:c.clientSecret,refresh_token:refresh,grant_type:'refresh_token'}),t=await jf('https://oauth2.googleapis.com/token',{method:'POST',headers:{'Content-Type':'application/x-www-form-urlencoded'},body:b});db.prepare('UPDATE accounts SET access=?,expires=? WHERE id=?').run(enc(t.access_token),Date.now()+Number(t.expires_in||3600)*1000,id);return t.access_token}
async function drive(id,url,opt={}){let h={...(opt.headers||{}),Authorization:`Bearer ${await token(id)}`};if(opt.body&&!h['Content-Type'])h['Content-Type']='application/json';return jf('https://www.googleapis.com'+url,{...opt,headers:h})}async function quota(id){let a=await drive(id,'/drive/v3/about?fields=storageQuota(limit,usage)'),q=a.storageQuota||{};db.prepare('UPDATE accounts SET quota=?,used=? WHERE id=?').run(q.limit===undefined?null:Number(q.limit),Number(q.usage||0),id)}
async function root(id){let a=account(id);if(a.root)return a.root;let q=encodeURIComponent("name='Farooqdrive' and mimeType='application/vnd.google-apps.folder' and 'root' in parents and trashed=false"),r=await drive(id,`/drive/v3/files?q=${q}&fields=files(id)&spaces=drive&pageSize=10`),rid=r.files?.[0]?.id;if(!rid)rid=(await drive(id,'/drive/v3/files?fields=id',{method:'POST',body:JSON.stringify({name:'Farooqdrive',mimeType:'application/vnd.google-apps.folder',parents:['root']})})).id;db.prepare('UPDATE accounts SET root=? WHERE id=?').run(rid,id);return rid}
function b64(b){return Buffer.from(b).toString('base64').replace(/=/g,'').replace(/\+/g,'-').replace(/\//g,'_')}async function oauth(){let c=cfg();if(!c.clientId||!c.clientSecret)throw Error('Save Google OAuth Client ID and Client Secret first.');let verifier=b64(crypto.randomBytes(48)),challenge=b64(crypto.createHash('sha256').update(verifier).digest()),st=b64(crypto.randomBytes(24));return new Promise((resolve,reject)=>{let server=http.createServer(async(req,res)=>{try{let u=new URL(req.url,'http://127.0.0.1');if(u.pathname!='/oauth2/callback')return res.writeHead(404).end();if(u.searchParams.get('state')!==st)throw Error('OAuth state validation failed.');if(u.searchParams.get('error'))throw Error('Google authorization: '+u.searchParams.get('error'));let code=u.searchParams.get('code'),redirect=`http://127.0.0.1:${server.address().port}/oauth2/callback`,body=new URLSearchParams({code,client_id:c.clientId,client_secret:c.clientSecret,redirect_uri:redirect,grant_type:'authorization_code',code_verifier:verifier}),t=await jf('https://oauth2.googleapis.com/token',{method:'POST',headers:{'Content-Type':'application/x-www-form-urlencoded'},body}),about=await jf('https://www.googleapis.com/drive/v3/about?fields=user(displayName,emailAddress),storageQuota(limit,usage)',{headers:{Authorization:`Bearer ${t.access_token}`}}),old=db.prepare('SELECT * FROM accounts WHERE email=?').get(about.user.emailAddress),refresh=t.refresh_token||(old?dec(old.refresh):'');db.prepare(`INSERT INTO accounts(email,name,access,refresh,expires,root,quota,used) VALUES(?,?,?,?,?,?,?,?) ON CONFLICT(email) DO UPDATE SET name=excluded.name,access=excluded.access,refresh=excluded.refresh,expires=excluded.expires,quota=excluded.quota,used=excluded.used`).run(about.user.emailAddress,about.user.displayName||about.user.emailAddress,enc(t.access_token),enc(refresh),Date.now()+Number(t.expires_in||3600)*1000,old?.root||null,about.storageQuota?.limit===undefined?null:Number(about.storageQuota.limit),Number(about.storageQuota?.usage||0));let id=Number(db.prepare('SELECT id FROM accounts WHERE email=?').get(about.user.emailAddress).id);await root(id);res.writeHead(200,{'Content-Type':'text/html'}).end('<h1>FarooqDrive connected</h1><p>Authorization is complete. Close this tab and return to FarooqDrive.</p>');setTimeout(()=>server.close(),200);resolve()}catch(e){res.writeHead(500).end(e.message);server.close();reject(e)}});server.listen(0,'127.0.0.1',async()=>{let redirect=`http://127.0.0.1:${server.address().port}/oauth2/callback`,u=new URL('https://accounts.google.com/o/oauth2/v2/auth');Object.entries({client_id:c.clientId,redirect_uri:redirect,response_type:'code',scope:SCOPE,access_type:'offline',prompt:'consent select_account',state:st,code_challenge:challenge,code_challenge_method:'S256'}).forEach(([k,v])=>u.searchParams.set(k,v));await shell.openExternal(u.toString())})})}
function mime(n){let e=n.toLowerCase().split('.').pop(),m={pdf:'application/pdf',txt:'text/plain',jpg:'image/jpeg',jpeg:'image/jpeg',png:'image/png',mp3:'audio/mpeg',mp4:'video/mp4',zip:'application/zip',json:'application/json',csv:'text/csv'};return m[e]||'application/octet-stream'}async function choose(size){for(let a of rawAccounts())try{await quota(Number(a.id))}catch{}let a=rawAccounts().filter(x=>x.quota===null||Number(x.quota)-Number(x.used||0)>=size).sort((x,y)=>(y.quota===null?1e30:Number(y.quota)-Number(y.used||0))-(x.quota===null?1e30:Number(x.quota)-Number(x.used||0)))[0];if(!a)throw Error('No connected Drive has enough free space.');return a}
async function uploadFile(p,folder){let st=fs.statSync(p),a=await choose(st.size),id=Number(a.id),rid=await root(id),tok=await token(id),name=path.basename(p),mt=mime(name),r=await fetch('https://www.googleapis.com/upload/drive/v3/files?uploadType=resumable&fields=id,name,mimeType,size,modifiedTime,webViewLink,parents',{method:'POST',headers:{Authorization:`Bearer ${tok}`,'Content-Type':'application/json','X-Upload-Content-Type':mt,'X-Upload-Content-Length':String(st.size)},body:JSON.stringify({name,parents:[rid]})});if(!r.ok)throw Error(`Upload session ${r.status}: ${await r.text()}`);let loc=r.headers.get('location');if(!loc)throw Error('Google did not return upload URL.');let meta=await new Promise((resolve,reject)=>{let u=new URL(loc),req=https.request({method:'PUT',hostname:u.hostname,path:u.pathname+u.search,headers:{'Content-Type':mt,'Content-Length':String(st.size)}},res=>{let d='';res.on('data',c=>d+=c);res.on('end',()=>res.statusCode>=200&&res.statusCode<300?resolve(JSON.parse(d||'{}')):reject(Error(`Google upload ${res.statusCode}: ${d}`)))});req.on('error',reject);let sent=0,s=fs.createReadStream(p);s.on('data',c=>{sent+=c.length;win.webContents.send('progress',{file:name,percent:Math.round(sent/st.size*100)})});s.on('error',reject);s.pipe(req)});db.prepare('INSERT INTO files(account,driveid,name,mime,size,folder,parent,modified,view) VALUES(?,?,?,?,?,?,?,?,?) ON CONFLICT(account,driveid) DO UPDATE SET name=excluded.name,size=excluded.size,modified=excluded.modified,view=excluded.view').run(id,meta.id,meta.name||name,meta.mimeType||mt,Number(meta.size||st.size),folder??null,rid,meta.modifiedTime||new Date().toISOString(),meta.webViewLink||'');await quota(id)}
function ensurePath(parts){let parent=null;for(let name of parts){let r=parent===null?db.prepare('SELECT id FROM folders WHERE name=? AND parent IS NULL').get(name):db.prepare('SELECT id FROM folders WHERE name=? AND parent=?').get(name,parent);if(!r){let x=db.prepare('INSERT INTO folders(name,parent) VALUES(?,?)').run(name,parent);parent=Number(x.lastInsertRowid)}else parent=Number(r.id)}return parent}async function children(id,parent){let out=[],page='';do{let q=encodeURIComponent(`'${parent}' in parents and trashed=false`),x=await drive(id,`/drive/v3/files?q=${q}&fields=nextPageToken,files(id,name,mimeType,size,modifiedTime,webViewLink,parents)&spaces=drive&pageSize=1000${page?'&pageToken='+encodeURIComponent(page):''}`);out.push(...(x.files||[]));page=x.nextPageToken||''}while(page);return out}async function sync(){let count=0;for(let a of rawAccounts()){let id=Number(a.id),q=[{id:await root(id),parts:[]}];while(q.length){let x=q.shift();for(let f of await children(id,x.id)){if(f.mimeType==='application/vnd.google-apps.folder'){q.push({id:f.id,parts:[...x.parts,f.name]});continue}let folder=ensurePath(x.parts);db.prepare('INSERT INTO files(account,driveid,name,mime,size,folder,parent,modified,view) VALUES(?,?,?,?,?,?,?,?,?) ON CONFLICT(account,driveid) DO UPDATE SET name=excluded.name,mime=excluded.mime,size=excluded.size,parent=excluded.parent,modified=excluded.modified,view=excluded.view').run(id,f.id,f.name,f.mimeType||'',Number(f.size||0),folder,f.parents?.[0]||x.id,f.modifiedTime||'',f.webViewLink||'');count++}}await quota(id)}return count}
function h(ch,fn){ipcMain.handle(ch,async(_e,...a)=>{try{return{ok:true,data:await fn(...a)}}catch(e){return{ok:false,error:e.message||String(e)}}})}app.whenReady().then(()=>{init();win=new BrowserWindow({width:1260,height:820,minWidth:980,minHeight:650,title:'FarooqDrive',icon:path.join(__dirname,'..','build','icon.ico'),webPreferences:{preload:path.join(__dirname,'preload.js'),contextIsolation:true,nodeIntegration:false,sandbox:false}});win.removeMenu();win.loadFile(path.join(__dirname,'renderer','index.html'));h('state',()=>state());h('oauth',x=>{if(!x.clientId?.trim()||!x.clientSecret?.trim())throw Error('Client ID and Secret are required.');setSecret('clientId',x.clientId.trim());setSecret('clientSecret',x.clientSecret.trim());return state()});h('add',async()=>{await oauth();return state()});h('disconnect',id=>{db.prepare('DELETE FROM accounts WHERE id=?').run(Number(id));return state()});h('folder',x=>{if(!x.name?.trim())throw Error('Folder name required.');db.prepare('INSERT INTO folders(name,parent) VALUES(?,?)').run(x.name.trim(),x.parent??null);return state()});h('move',x=>{db.prepare('UPDATE files SET folder=? WHERE id=?').run(x.folder??null,Number(x.id));return state()});h('sync',async()=>({count:await sync(),state:state()}));h('upload',async folder=>{let s=await dialog.showOpenDialog(win,{properties:['openFile','multiSelections']});if(s.canceled)return{canceled:true};for(let p of s.filePaths)await uploadFile(p,folder);win.webContents.send('progress',{done:true});return{canceled:false,state:state()}});h('rename',async x=>{let f=db.prepare('SELECT * FROM files WHERE id=?').get(Number(x.id));await drive(Number(f.account),`/drive/v3/files/${encodeURIComponent(f.driveid)}?fields=id,name`,{method:'PATCH',body:JSON.stringify({name:x.name})});db.prepare('UPDATE files SET name=? WHERE id=?').run(x.name,Number(x.id));return state()});h('trash',async id=>{let f=db.prepare('SELECT * FROM files WHERE id=?').get(Number(id));await drive(Number(f.account),`/drive/v3/files/${encodeURIComponent(f.driveid)}?fields=id,trashed`,{method:'PATCH',body:JSON.stringify({trashed:true})});db.prepare('DELETE FROM files WHERE id=?').run(Number(id));return state()});h('preview',async id=>{let f=db.prepare('SELECT view FROM files WHERE id=?').get(Number(id));if(!f?.view)throw Error('No Google preview link for this file.');await shell.openExternal(f.view);return true});h('download',async id=>{let f=db.prepare('SELECT * FROM files WHERE id=?').get(Number(id)),s=await dialog.showSaveDialog(win,{defaultPath:f.name});if(s.canceled)return{canceled:true};let tok=await token(Number(f.account)),r=await fetch(`https://www.googleapis.com/drive/v3/files/${encodeURIComponent(f.driveid)}?alt=media`,{headers:{Authorization:`Bearer ${tok}`}});if(!r.ok)throw Error(`Download ${r.status}: ${await r.text()}`);let{Readable}=require('stream');await new Promise((ok,no)=>Readable.fromWeb(r.body).pipe(fs.createWriteStream(s.filePath)).on('finish',ok).on('error',no));return{canceled:false,path:s.filePath}})});app.on('window-all-closed',()=>{if(process.platform!=='darwin')app.quit()});
